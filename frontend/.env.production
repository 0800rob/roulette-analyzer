# Production environment for the Vite build (used by `npm run build` on Vercel).
# This URL is baked into the JS bundle when the frontend is deployed.

VITE_API_URL=https://roulette-analyzer.fly.dev/api
